global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'meno-api'
    scrape_interval: 5s
    honor_labels: true
    static_configs:
      - targets: [ 'meno-api:8080', 'host.docker.internal:8080' ]
